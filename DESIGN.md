# DESIGN

## 디자인 철학
- 빠르게 스캔 가능한 정보 밀도
- 과한 장식보다 명확한 우선순위
- 티꿀 브랜드 톤: 노랑/앰버 포인트 + 신뢰감 있는 슬레이트 기반

## 컴포넌트 재사용 규칙
- 공통 UI는 `src/components/shared` 우선 사용
- 동일 레이아웃 패턴은 기존 카드/배지/필 버튼 스타일을 재사용
- 새 컴포넌트는 도메인별 디렉터리에 추가하고 props 인터페이스 명확히 정의

## 색상/간격 시스템
- 기본 텍스트: `slate-900`, 보조 텍스트: `slate-500`
- 포인트: `amber-400~600`
- 경고/에러: `rose-600` 계열
- 기본 간격 단위: `0.25rem` 기반(4px grid)

## 타이포그래피
- 제목: 강한 굵기(700~900), 짧고 명확한 라벨
- 본문: 14px~16px 중심
- 보조 정보: 12px~13px

## 반응형 기준
- 모바일 우선 설계
- 2열 이상 레이아웃은 `md` 이상에서만 적용
- 긴 텍스트는 `truncate` 처리

## 인터랙션 규칙
- 클릭 가능한 요소는 hover/focus 상태 제공
- 드롭다운/모달은 외부 클릭 시 닫힘 처리
- 저장/요청 버튼은 로딩 상태를 텍스트로 명시

## 금지 사항
- 페이지별 임의 팔레트 남발 금지
- 동일 기능의 버튼 스타일 중복 정의 금지
- 기존 정보 구조를 깨는 과도한 애니메이션 금지
